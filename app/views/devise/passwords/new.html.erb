<h2 style="margin-left:12rem;margin-top:2rem;">Recuperer mon mot de passe</h2>
<div class="text-center" style="height: 100%;display:flex;align-items: center;
  padding-top: 6rem;
  padding-bottom: 6rem;
  background-color: #f5f5f5;width:50%;margin-top:3rem;margin-left:25%;">
  <main class="form-signin" style="width: 100%;
  max-width: 330px;
  padding: 15px;
  margin: auto;font-weight: 400;z-index: 2;">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group" style="margin-bottom:1rem;">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email",class:"form-control", style:"margin-bottom: 1rem;
  border-bottom-right-radius: 0;
  border-bottom-left-radius: 0;" %>
      </div>

      <div class="form-group">
        <%= f.submit "Demande de renéinitialisation de mot de passe", class:"btn btn-primary" %>
      </div>
    <% end %>

  <%= render "devise/shared/links" %>
  </main>
</div>
