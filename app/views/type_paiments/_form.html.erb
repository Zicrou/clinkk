<div class="container portfolio-container">
  <%= form_with(model: type_paiment, local: true) do |form| %>
    <% if type_paiment.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(type_paiment.errors.count, "error") %> prohibited this type_paiment from being saved:</h2>

        <ul>
        <% type_paiment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group col-md-4">
      <%= form.label :Type %>
      <%= form.text_field :name, class:"form-control" %>
    </div>
  <br>
    <div class="form-group">
      <%= form.submit "Valider", class:"btn btn-primary" %>
    </div>
  <% end %>
</div>