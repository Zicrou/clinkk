<div class="container portfolio-container">
  <%= form_with(model: ipm, local: true) do |form| %>
    <% if ipm.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(ipm.errors.count, "error") %> prohibited this ipm from being saved:</h2>

        <ul>
        <% ipm.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group col-md-4">
      <%= form.label :name %>
      <%= form.text_field :name, class:"form-control" %>
    </div>

  <br>
    <div class="form-group">
      <%= form.submit "Valider", class:"btn btn-primary" %>
    </div>
  <% end %>
</div>